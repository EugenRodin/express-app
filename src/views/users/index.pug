doctype html
html
    head
        title Users
        link(rel='stylesheet', href='/css/styles.css')
    body
        h1 Users List
        ul
            each user in users
                li: a(href=`/users/${user.id}`) #{user.name}